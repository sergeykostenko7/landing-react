# S-Pro Landing Page (ReactJS version)

### Installing & Running the App
* Run `yarn install` to install the dependencies
* Run `npm run server` to build the app and launch the development server
* Open http://localhost:8080
